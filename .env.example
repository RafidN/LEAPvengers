# LEAPvengers environment template
#
# Create a local .env file in the project root before running the app.
# Example (PowerShell): Copy-Item .env.example .env
# Then replace placeholder values with your real local settings.
#
# Note: .env is gitignored and should not be committed.

# Backend


# Auth


# Frontend
